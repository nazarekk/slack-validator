Application allows to validate slack token for specified scopes.

To provide slack token and scopes define them in application arguments, where first value should be token and next N values should be scopes which have to be validated
